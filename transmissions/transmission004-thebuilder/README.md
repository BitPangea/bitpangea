# Transmission 004 — The Builder

## A BitPangea Transmission to the Builder

This directory contains **Transmission 004 — The Builder**, a BitPangea transmission record presented as a preserved visual artifact.

The source page is intentionally minimal: its visible content consists of a single image artifact rather than a prose transmission, explanatory essay, or architectural statement.

---

## Public / Canonical Path

Canonical URL:

**https://bitpangea.com/transmission/transmission004-thebuilder/index.html/**

Repository path represented by the canonical URL:

```text
/transmission/transmission004-thebuilder/index.html
```

Recommended directory README path:

```text
/transmission/transmission004-thebuilder/README.md
```

The source page is publicly indexable:

```text
index,follow
```

---

## Record Identity

The page metadata identifies:

```text
Title: Transmission 004 - The Builder | BitPangea
Description: Transmission 004 - The Builder | a BitPangea transmission to the builder.
```

The structured-data description is:

> **A BitPangea transmission to the builder.**

No additional visible explanatory text is supplied by the source page.

---

## Preserved Artifact

The visible page centers a single image:

```text
/transmissions/transmission004-thebuilder/transmission004-builder.jpg
```

Its alt text is:

> **Transmission 004 - The Builder | BitPangea — A BitPangea transmission to the builder.**

The image is therefore the substantive preserved artifact of this page.

---

## Documentary Boundary

Because the source page provides no visible transmission text beyond the image itself, this README should not infer:

- what “The Builder” is intended to mean beyond the page title and metadata;
- what instructions, doctrine, or philosophy the image contains;
- whether the transmission defines current Builder responsibilities;
- whether the record establishes current Atlas Architecture, Codex law, or implementation rules.

The page should be treated as a preserved visual transmission unless and until the source itself contains additional documentary content.

---

## Path Distinction

The source page contains a path distinction that should be preserved rather than silently normalized.

Canonical page path:

```text
/transmission/transmission004-thebuilder/index.html/
```

Artifact image path:

```text
/transmissions/transmission004-thebuilder/transmission004-builder.jpg
```

The singular `/transmission/` and plural `/transmissions/` forms both appear in the source.

This README records that distinction exactly rather than assuming one is an error.

---

## Metadata Identity

The source includes:

```text
Theme Color: #00c8d7
Robots: index,follow
Canonical: https://bitpangea.com/transmission/transmission004-thebuilder/index.html/
OG Type: article
OG Image: https://bitpangea.com/images/bitpangea-og.jpg
Twitter Card: summary_large_image
```

Structured data identifies the page as a `CreativeWork` that is part of the BitPangea website.

These metadata values should remain aligned with the source unless intentionally revised.

---

## Presentation

The source page defines an extensive transmission visual framework but currently renders only the centered artifact image and footer copy.

The visible presentation includes:

- near-black background;
- gold and cyan design tokens;
- centered artifact image;
- restrained gold border around the image;
- BitPangea footer copyright line.

The source footer reads:

```text
© 2026 BitPangea · Built on Cryptographic Permanence
```

That footer wording is part of the source page and should be preserved unless the page itself is intentionally updated.

---

## Repository Guidance

When maintaining this directory:

1. preserve the title **Transmission 004 — The Builder**;
2. preserve the canonical URL `https://bitpangea.com/transmission/transmission004-thebuilder/index.html/`;
3. preserve the page’s singular `/transmission/` canonical path unless intentionally revised;
4. preserve the artifact image path `/transmissions/transmission004-thebuilder/transmission004-builder.jpg`;
5. preserve the distinction between the singular page path and plural image path rather than silently reconciling them;
6. preserve the description **A BitPangea transmission to the builder.**;
7. preserve the artifact alt text unless the image itself is intentionally replaced;
8. preserve `index,follow` unless the publication model is intentionally revised;
9. keep canonical, Open Graph, Twitter, description, theme-color, favicon, and structured-data metadata aligned with the record;
10. preserve the page as a visual transmission record unless additional source content is deliberately added;
11. do not invent prose, doctrine, Builder responsibilities, or architectural meaning not present in the source;
12. do not treat this record as automatically controlling current BitPangea Builder architecture or policy;
13. preserve the source footer wording unless the page itself is intentionally updated.

---

## Status

**BitPangea Transmission**

**Transmission 004 · The Builder**

**Visual Record**

---

© 2026 BitPangea. All rights reserved unless otherwise stated.
